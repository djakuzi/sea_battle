import { ServiceServerConnectionStatus } from "@app-network/webSocket/services/ServerConnectionStatus/types/ServerConnectionStatus.service";

export interface PropsPing {
    cls: string;
    value: number | null;
}