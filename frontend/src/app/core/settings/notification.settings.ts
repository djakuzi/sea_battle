import { IntrNotificationConfig } from "./types/notification.interface";

const hide: number = 7000;

/**
 * @param {number} hide - время скрытия уведомления
 */
export const CONFIG_NOTIFICATION: IntrNotificationConfig = {
    hide,
};